import { Flex, Text, Button } from "@radix-ui/themes";

export default function App() {
  return (
    <Flex direction="column" gap="5">
      <Text>Deploy Repo to Cloudflare</Text>
      <Button>Let's go</Button>
    </Flex>
  );
}
